# AI 分析契约

本目录只包含 Pydantic Schema、示例 JSON、版本化 Prompt 和后续模块边界，不连接任何真实大模型。

- `schemas/`：评论分析与问题聚类的严格输入输出。
- `prompts/`：只基于输入、证据原文引用、官方回复与物流内容隔离等约束。
- `classifiers/`、`clustering/`、`evaluation/`：后续阶段入口说明。
