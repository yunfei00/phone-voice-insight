# 采集器层

`base` 定义与 Django 任务层解耦的输入、原始页、原始记录、标准化反馈、checkpoint、校验结果和错误契约。`jd` 与 `honor_club` 当前只提供未实现骨架，不访问外部网站，也不会生成虚假数据。

采集实现必须遵守平台条款、robots/公开接口约束、最小必要原则与合理限速；敏感凭据不得进入日志或仓库。详细约束见 `docs/crawler-contract.md`。
